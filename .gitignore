.DS_Store
node_modules/
*.log
.env
.env.*
.superpowers/

# ADLC state: config.json is shared (names the PM team/project); everything
# else under .adlc/ is per-machine and stays local
.adlc/*
!.adlc/config.json

# Claude Code local state (settings.json IS committed; local overrides are not)
.claude/settings.local.json
.claude/worktrees/
.worktrees/

# Local working docs / session notes — never committed; canonical specs live
# on the task's Linear issue (METHODOLOGY.md idea 4)
docs/design/
docs/plans/
