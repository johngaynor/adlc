# {{TITLE}}

- **Date**: {{YYYY-MM-DD}}
- **Linear issue**: {{ISSUE_URL}}
- **PR(s)**: {{PR_LINKS}}

## What shipped

The concrete, user-visible outcome — what changed, in plain terms.

## Why

The problem this solved and why it mattered. A sentence or two, not the full
`## Idea`/`## Specification` restated.

## How

The approach in brief — the key decision or two, not a phase-by-phase replay of
`## Plan`.

## Notable deviations from the plan

Anything that changed from the agreed `## Plan` during execution, and why. Say
"None." if the work matched the plan as approved.

---

> Curated, not verbatim: a light, readable record for future readers — not a copy
> of the issue's `## Specification` or `## Plan`. Archived summaries live in
> `docs/adlc/{YYYY-MM-DD}-{kebab-slug}.md`.
