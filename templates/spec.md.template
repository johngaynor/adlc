# {{TITLE}}

- **Date**: {{YYYY-MM-DD}}
- **Status**: draft | in-progress | implemented
- **Author**: {{AUTHOR}}

## Problem / Goal

What are we solving, and why now? What does "done" look like from the user's side?

## Approach

The chosen design in a few paragraphs. Note the key trade-offs and what was
rejected. Keep it honest about risk and blast radius.

## Phases

Break the work into ordered, independently-committable phases.

1. **Phase 1 — <name>**: …
2. **Phase 2 — <name>**: …

## Test Coverage

List the tests this change adds or updates — the API paths, UI flows, and edge
cases exercised. A feature is not specced until its verification is specced.

## Backward Compatibility

Any contract surface touched (types, signatures, routes, schema, events)? If so,
the migration/deprecation plan. If none, say "No contract surfaces affected."

## Open Questions

Anything needing a human decision before or during implementation.

---

> Specs live in `.ai/specs/{YYYY-MM-DD}-{kebab-title}.md`. Update the spec's
> **Status** as the work progresses so the repo records intent, not just outcome.
